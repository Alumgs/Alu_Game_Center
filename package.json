{
  "name": "alu-play-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "ALU PLAY private game library on Cloudflare Workers + D1 + R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
